import { defineConfig } from "vite";

export default defineConfig({
  // IMPORTANT: set to your repo name when deploying
  // e.g. https://username.github.io/transit-plots/ => base: "/transit-plots/"
  base: "/transit-plots/"
});
